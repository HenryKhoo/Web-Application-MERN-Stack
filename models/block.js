// var mongoose = require('mongoose');
// var Schema = mongoose.Schema;

// var blockSchema = mongoose.Schema({data : JSON});
// var post_model = mongoose.model('blocks', blockSchema);


// //var block = mongoose.model('blocks', blockSchema);

// module.exports = post_model;

